apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: backend-rb
  namespace: application

subjects:
  - kind: ServiceAccount
    name: backend-sa
    namespace: application

roleRef:
  kind: Role
  name: backend-role
  apiGroup: rbac.authorization.k8s.io
